// GitHub username — used for GitHub API (display name fallback)
export const githubUsername = "st-wh";

// Canonical display name — used as hero h1 if GitHub API name is unavailable
export const siteOwnerName = "Stanton Wortham";

// Canonical title — two lines rendered below h1 in the hero
export const siteOwnerTitle = [
  "Charles F. Donovan, S.J. Dean",
  "Lynch School of Education and Human Development, Boston College",
];

// CV PDF — set to "/files/cv.pdf" after dropping the file in public/files/
// See public/files/README.txt for instructions.
// When null, the CV nav link is hidden entirely.
export const cvUrl = null;

// Boston College faculty profile — verified link
export const bcProfileUrl =
  "https://www.bc.edu/bc-web/schools/lynch-school/faculty-research/faculty-directory/stanton-wortham.html";
