// GitHub username — used for API data (name, bio, avatar)
export const githubUsername = "st-wh";

// Navbar logo — null uses default icon; replace with imported image when available
export const navLogo = null;

// Hero tagline — one-line research statement shown below name in hero
// TODO: Add verified research statement
export const heroTagline = null;

// About section — supplementary bio paragraph shown below GitHub bio
// TODO: Add extended bio text when available
export const moreInfo = null;

// CV / Résumé — link to downloadable PDF when available
// TODO: Add verified CV link
export const resume = null;

// Footer social links — theme for footer icon colors ("light" or "dark")
export const footerTheme = "dark";

// Blog / external link shown in footer social links
export const Blog = null;

// Kept for compatibility — not currently displayed
export const skillData = [];
export const filteredProjects = [];
export const projectCardImages = [];
export const formspreeUrl = null;
